# Apple-Design v0.2 开发计划

## 目标

把“参考设计原则”变成能跨 session、跨 agent 复用的可验证约束：已批准设计不被悄悄重做，现有系统不被平行实现替代，探索不会被误报为完成。

## 本期（v0.2，正在交付）

| 目标 | 交付 | 验收 |
| --- | --- | --- |
| 防止设计漂移 | `LOCKED`、`EXISTING`、`EXPLORE` 状态和切换规则 | Skill 对每个重要表面先分类；LOCKED 场景不接受无授权重设计 |
| 统一跨 session 交付 | 可裁剪的交付合同 | 多状态/页面/共享组件的结果有证据、边界、状态、验证与未验证项 |
| 防止说明退化 | 六个独立行为评测场景及静态完整性检查 | `npm run test:evaluations` 通过；人工回归能发现越权改设计、伪造平台证据或忽略恢复状态 |

## 后续阶段

### v0.3：公共入口与协作

- 将英文作为简洁主入口，保留中文内容为同等语义的参考；验收为公开 README 与所有入口不再出现语言/能力矛盾。
- 深化 Figma 交接到变量、组件状态、Auto Layout、命名和 token 映射；不在没有 Figma 环境时声称已连接、创建或发布文件。
- 以 WCAG 2.2 AA 为 Web 项目可选择的明确验收基线，增加键盘、焦点、语义与人工读屏证据位置；不把自动检查称为认证。

### v0.4：平台与维护证据

- 为 Windows、iOS/iPadOS、macOS 分别建立“Web/壳/模拟器/真机”的证据矩阵；只有实际运行的平台才能获得通过状态。
- 在开始接收外部贡献时，增加 CI 执行 `check`、unit、portable 和 evaluation 完整性检查，并提供 issue/PR 模板。

## 本期不做

不新增未经验证的原生示例、不复制 Apple 受限资产、不增加模型 API 或自动评分服务、不把六个评测场景伪装成所有 agent 都已运行过的认证。
